module.exports = {
  // Custom TypeScript configuration
  typescript: {
    // Override TypeScript configuration options
    ignoreBuildErrors: true, // Ignore TypeScript errors during the build process
  },
};
